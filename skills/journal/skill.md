Capture an end-of-session work journal entry.

Review what was accomplished in this session by looking at recent git history, open files, and conversation context. Then create or append to a journal entry at:

$APHELOCOMA_HOME/core/journal/YYYY-MM/YYYY-MM-DD.md

Use today's date. Create the month directory if it doesn't exist.

Use the entry format from the template at `${CLAUDE_SKILL_DIR}/templates/entry.md`.

If the file already exists, append the new entry (don't overwrite previous entries from the same day).
