# Conventions

> Stub — written right after Checkpoint 1 (direction + stack picked) by the software-architect, or the
> nearest covering role (PROTOCOL §2 Phase 1). Replace this file's contents with the project's binding
> conventions: the chosen stack plus the ~10 choices implementers must match — structure, naming,
> state management, error style, key libraries, test layout. **One page max — a floor, not a style
> guide.** For an existing codebase, derive it from the Discovery survey (record what already exists).
> Implementers read it before building; the CP4 critique checks consistency against it (CRAFT.md
> principle 2).

_No active project yet._
