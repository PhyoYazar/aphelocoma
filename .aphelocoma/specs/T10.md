# T10 — Correct the v0.3 documentation assertions for the status board

## Goal

Make `docs/documentation-assertions-v0.3.md` true again. T9 added the `aph status` command and 28
tests; the release-readiness audit still describes the CLI surface and test list from before it.

## Scope

- DOC-002: the asserted public command list, which currently omits `status` while citing
  `test_cli.CliSubprocessTests.test_help_exposes_only_hamilton_commands` as its proof — the very test
  T9 changed.
- DOC-006: the test-ID list, now incomplete.
- Any other assertion in that file that T9 made stale — check the whole document, not only the two
  the reviewer named.

## Non-scope

- Changing `aph status` behavior, the board's content, or `PROTOCOL.md` §5.6. T9 is reviewed and done;
  this task describes what shipped, it does not redesign it.
- README or CHANGELOG, which T9 already updated.
- Version bump, tag, push, or release.

## Interfaces / files touched

- `docs/documentation-assertions-v0.3.md`
- `.aphelocoma/ledger/agents/technical-writer.md`

## Acceptance criteria

- [ ] DOC-002's command list includes `status` and its cited proof matches what the test now asserts.
- [ ] DOC-006's test-ID list matches the tests that exist in the repository.
- [ ] Every other assertion in the file is re-checked against current behavior; any that T9 made stale
  is corrected, and the check is stated in the handoff so the reviewer knows the sweep happened.
- [ ] Each assertion marked proven names a check that genuinely proves it — no claim is marked `[x]`
  on the strength of a test that does not cover it.
- [ ] The full standard-library suite, the Hamilton validator, and `aph doctor` still pass.

Note: this document is not machine-consumed — no test and no validator reads it, so nothing fails
while it is stale. That is exactly why it needs a deliberate pass before v0.3 ships.
